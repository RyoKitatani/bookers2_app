<h1>Books search for <%= "#{}"%></h1>
<table class="table table-hover table-inverse">
    <thread>
        <tr>
            <th></th>
            <th>Title</th>
            <th>Opinipn</th>
            <th colspan="5"></th>
        </tr>
    </thread>
    <tbody>
      <% @books.each do |book| %>
        <tr>
          <td>image</td>
          <td><%= book.title %></td>
          <td><%= book.opinion %></td>
        </tr>
      <% end %>
    </tbody>
</table>