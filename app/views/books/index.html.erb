
<%= render 'books/list', book: @book, user: @user %>

<h1>Books</h1>
<table>
    <thread>
        <tr>
            <th>Title</th>
            <th>Opinipn</th>
        </tr>
    </thread>
    <tbody>
        <% @books.each do |book| %>
            <tr>
            <td>
              <%= link_to book_path(book.id) do %>
                <%= book.title %>
              <% end %>
            </td>
                <td><%= book.body %></td>
            </tr>
        <% end %>
        </tbody>
    </table>
    <%= paginate @books %>
</div>
</div>
</div>
